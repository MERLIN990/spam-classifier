# Evidencia visual (capturas de pantalla)

Agrega aquí, tras ejecutar el proyecto en tu máquina, capturas de:

1. `docker build` completado sin errores (terminal).
2. `docker ps` mostrando el contenedor `healthy`.
3. El frontend (`http://localhost:8000/`) clasificando un mensaje de spam y uno de ham.
4. Swagger UI (`http://localhost:8000/docs`) con el endpoint `/api/predict` expandido.
5. `pytest -v` con todas las pruebas en verde.
6. El servicio ya desplegado en Render (`https://<tu-servicio>.onrender.com`).

Estas capturas son la "evidencia de integración (API, endpoints, entre
otros)" y de ejecución local que pide la rúbrica de la Fase 2.
